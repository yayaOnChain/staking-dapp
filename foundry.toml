[profile.default]
src = "src/contracts"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
cache_path = "cache/forge"

# Additional profiles
[profile.ci]
fuzz = { runs = 5000 }
